# Production dependencies (included in root requirements.txt)
fastapi==0.104.1
uvicorn==0.24.0
requests==2.31.0
python-dotenv==1.0.0

# Development dependencies (can be split into requirements-dev.txt for cleaner production)
-r requirements-dev.txt
